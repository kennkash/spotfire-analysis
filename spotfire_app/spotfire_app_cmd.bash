npm create vite@latest spotfire-license-hub -- --template react-ts
cd spotfire-license-hub
npm install


npx shadcn@latest init

When prompted, choose something like:

Framework: Vite

TypeScript: Yes

Style: Default

Base color: Slate (or whatever you want)

CSS file: src/index.css

Tailwind config: tailwind.config.ts

Components: src/components

Utils: src/lib/utils.ts

React Server Components: No

This sets up Tailwind + tokens + the cn() helper the official way.


npx shadcn@latest add button
npx shadcn@latest add card
npx shadcn@latest add select
npx shadcn@latest add table
npx shadcn@latest add dropdown-menu
npx shadcn@latest add separator

npm install next-themes



